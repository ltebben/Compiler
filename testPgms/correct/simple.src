program test_program is
    type fibb_result is integer;
    global variable x : integer;
    variable i : integer;
    variable q : integer;
    procedure fibb : integer (variable n: integer)
    begin
        if (n == 12) then
            x := 1;
        else
            x := 2;
            x := x+n;
        end if;
        return x+3;
    end procedure;
begin
    fibb_result := fibb(12);
    q := 5;
    for(i := 5; i < fibb_result)
        x := x + i;
    end for;


end program.
